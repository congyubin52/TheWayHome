<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.btc.thewayhome.user.board.comment.ICommentDaoMapper">

    <!--댓글 - 작성-->
    <insert id="insertComment" parameterType="com.btc.thewayhome.user.board.comment.CommentDto">
        INSERT INTO
            tbl_board_comment(b_type, b_no, u_m_id, b_c_content, b_c_reg_date)
        VALUES
            (#{b_type}, #{b_no}, #{u_m_id}, #{b_c_content}, NOW())
    </insert>

    <!--댓글 - 전체 리스트-->
    <select id="selectCommentAll" resultType="hashMap">
        SELECT
            *
        FROM
            tbl_board_comment
        WHERE
            b_type = "2" AND b_no =#{b_no}
    </select>

</mapper>